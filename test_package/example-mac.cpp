#include <iostream>
// #include "TFEL/Config/GetTFELVersion.h"

int main() {
    // getTFELVersion();
    // Linking fails with
    // : can't link with bundle (MH_BUNDLE) only dylibs (MH_DYLIB) file
    //'../lib/libLicosMFrontModel.so' for architecture x86_64
}
